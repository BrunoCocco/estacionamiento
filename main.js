//TODO:
// vamos a hacer un sistema para un estacionamiento de autos.
// definir posiciones.
// seleccionar una posicion y crear un objeto para entregar el ticket
//crear la card de pa persona y que esta empiece a correr el tiempo corresponediente para poder computar el cobro.
//opcion cobrar.
//cerrar posicion y que vuelva a estar disponible.


class Usuario{
    constructor(nombre,patente,horaInicio,id,estado){
        this.name = nombre,
        this.patenta = patente,
        this.time = horaInicio,
        this.id = id,
        this.estado = estado
    }
}
//TODO: lo proximo
const posiciones = [];


function ejectPosiciones() {
        let i = new Usuario
        console.log(i)
        for (i = 0; i < 15; i++) {
            posiciones.push(i);
            this.id = i
            }
            console.log(posiciones);
            return
        }
ejectPosiciones()




